import 'package:flutter/material.dart';
import 'package:my_project/widgets/custom_app_body.dart';

class TabOfNotification extends StatelessWidget {
  const TabOfNotification({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CustomAppBody(children: []);
  }
}
