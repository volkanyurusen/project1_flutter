import 'package:flutter/material.dart';

import 'bodies/zbodies.dart';

class ScreenOfProfile extends StatelessWidget {
  // static const String routeName = 'screen_of_profile';

  const ScreenOfProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return ProfileBody();
  }
}
