import 'package:flutter/material.dart';

import 'bodies/zbodies.dart';

class TabOfTrainingRequest extends StatelessWidget {
  const TabOfTrainingRequest({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TrainingRequestBody();
  }
}
