import 'package:flutter/material.dart';

import 'bodies/zbodies.dart';

class TabOfTrainingOffer extends StatelessWidget {
  const TabOfTrainingOffer({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TrainingOfferBody();
  }
}
