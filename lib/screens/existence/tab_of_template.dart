import 'package:flutter/material.dart';

import 'bodies/zbodies.dart';

class TabOfTemplate extends StatelessWidget {
  const TabOfTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TemplatesBody();
  }
}
