import 'package:flutter/material.dart';

class ValidationBox extends StatelessWidget {
  const ValidationBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
